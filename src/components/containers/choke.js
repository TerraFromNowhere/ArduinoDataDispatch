import React from 'react';
import {StyledChoke} from './styledContainers/sComponents';
import BigSpinner from '../img/Bigspinner.png';



export const Choke = () => {
    return (
        <StyledChoke src = {`${BigSpinner}`}>
           
        </StyledChoke>
    );
}